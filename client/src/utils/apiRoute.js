const host = "http://localhost:8080";
const registerRoute = `${host}/api/register`;
const loginRoute = `${host}/api/login`
const contactsRoute = `${host}/api/allusers`
// const avatarRoute = `${hist}/api/avatar`
module.exports ={
    host,
    registerRoute,
    loginRoute,
    contactsRoute
}